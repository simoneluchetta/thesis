# diagnostics/

`court_pnp_cameras_redfox_repaired.json` holds the lens of each of the ten cameras around the
court: focal length, image centre and distortion, for 3840 x 2160 frames.

It belongs together with `../placements/placed_cameras_redfox_repaired.json`, which says where
each camera is. The two were solved together; always use them as a pair.
